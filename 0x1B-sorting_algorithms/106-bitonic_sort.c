#include "sort.h"

/**
 * bitonic_sort - Sorting an array of numbers using bitonic sort
 * @array: Array
 * @size: Size
 * Return: Nothing
**/

void bitonic_sort(int *array, size_t size)
{
	if (array == NULL || size < 2)
		return;
}
