#include "sort.h"

/**
 * heap_sort - Sorting an array of numbers using heap sort
 * @array: Array
 * @size: Size
 * Return: Nothing
**/

void heap_sort(int *array, size_t size)
{
	if (array == NULL || size < 2)
		return;
}